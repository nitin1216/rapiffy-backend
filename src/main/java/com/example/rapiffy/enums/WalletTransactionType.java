package com.example.rapiffy.enums;

public enum WalletTransactionType {
    CREDIT,  // Money added to wallet (e.g. return refund)
    DEBIT    // Money deducted from wallet (e.g. used during checkout)
}
